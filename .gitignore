*.iml
.kotlin
.gradle
**/build/
!composeApp/src/**/kotlin/com/nuvio/app/core/build/
!composeApp/src/**/kotlin/com/nuvio/app/core/build/**
xcuserdata
!src/**/build/
local.properties
.idea
.DS_Store
captures
.externalNativeBuild
.cxx
*.xcodeproj/*
!*.xcodeproj/project.pbxproj
!*.xcodeproj/xcshareddata/
!*.xcodeproj/project.xcworkspace/
!*.xcworkspace/contents.xcworkspacedata
**/xcshareddata/WorkspaceSettings.xcsettings
node_modules/
logs/
analysis/
Docs
SUPPORTER_COSMETICS.md
# NOTE: upstream ignores supabase/migrations/ (they keep their schema private).
# This fork version-controls the backend -- see CLAUDE.md "Supabase backend is
# tracked in this repo" -- and the Supabase Database Tests workflow reads these
# migrations, so the ignore is deliberately NOT carried over. Re-drop it if an
# upstream sync reintroduces it.
keystore/
scripts/build-distribution.sh
asset
scripts/scrape_android_compose_animation_docs.py
tools
AGENTS.md

PERFORMANCE_FINDINGS.md
# NOTE: upstream added a blanket `scripts` ignore. This fork version-controls its
# release tooling under scripts/ (set-version.sh, generate-release-notes.sh,
# release-metadata.sh -- see CLAUDE.md), and the desktop/Android release flows
# depend on new scripts being tracked, so the blanket ignore is NOT carried over.
# Re-drop it if an upstream sync reintroduces it.

# Local MPVKit iOS build environment (sparse APFS image, see MPVKit docs)
.mpvkit-build.sparseimage

dev/
